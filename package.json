{
  "name": "notepad",
  "private": true,
  "type": "module",
  "description": "Encrypted single-phrase notepad. No dependencies — the browser loads Supabase and DOMPurify from a CDN at runtime.",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
